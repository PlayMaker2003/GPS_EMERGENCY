<resources>
    <!-- Estilo personalizado para el Spinner -->
    <style name="SpinnerTextStyle" parent="Widget.AppCompat.Spinner">
        <item name="android:textColor">#000000</item> <!-- Texto negro -->
        <item name="android:textSize">16sp</item> <!-- Tamaño del texto -->
    </style>

    <!-- Estilo para los elementos del menú desplegable -->
    <style name="SpinnerDropDownItemStyle" parent="Widget.AppCompat.Spinner.DropDown">
        <item name="android:textColor">#000000</item> <!-- Texto negro -->
        <item name="android:textSize">16sp</item> <!-- Tamaño del texto -->
    </style>
</resources>
